{
  "name": "crypto-revenue-metrics",
  "version": "1.0.0",
  "description": "Subgraph to track revenue metrics for crypto protocols",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio lido-revenue-metrics",
    "create-local": "graph create --node http://localhost:8020/ lido-revenue-metrics",
    "remove-local": "graph remove --node http://localhost:8020/ lido-revenue-metrics",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 lido-revenue-metrics"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.67.2",
    "@graphprotocol/graph-ts": "^0.31.0"
  }
}
